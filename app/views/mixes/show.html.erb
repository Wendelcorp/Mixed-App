<%= link_to 'All mixes', mixes_path %>
<%= render 'song_form' %>

<% @songs.each do |song| %>
  <% next if song.new_record? %>
  <%= song.artist %>
  <%= song.name %>
  <%= song.source %>
  <%= link_to 'Delete', song, method: :delete, data: { confirm: 'Are you sure?' } %>
  <br>
<% end %>
